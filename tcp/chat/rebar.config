{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
    {config, "config/sys.config"},
    {apps, [chat_launcher]}
]}.

{escript_main_app, chat_client}.
{escript_name, "chat_client"}.
{escript_emu_args, "%%! -escript main chat_client -noshell\n"}.
